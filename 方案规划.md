# PoC Workbench — 方案规划 v3

> 目标：本地 SQLite 的个人 PoC 存储管理工具。
>
> **v3 核心转向（你拍板的）：不做「元数据+原文托管」，做工具自有的原生模板格式 PWF——类似 Nuclei/Xray 那样可直接执行的模板语言。管理对象是数据库里的 PoC 记录，不是文件。新增 PoC 不需要任何文件：粘贴 Xray 自动转换，或表单逐项手填。扫描验证由自研引擎直接执行 PWF，不依赖外部 nuclei/xray 二进制。**

---

## 一、原生格式 PWF（PoC Workbench Format）

### 1.1 设计依据

| 事实 | 推论 |
|---|---|
| Xray 表达式是 Go 语法子集（`response.status == 200 && response.body.bcontains(...)`） | 后端就是 Go，用 `github.com/expr-lang/expr` 注册自定义函数即可直接求值——**自研执行引擎可行且成本低** |
| Xray 结构（transport/rules/expression）表达力覆盖 http+tcp | PWF 以 Xray 结构为骨架，元数据提升到统一 metadata 块 |
| 你的真实样例元数据稀疏脏（product 空、name 随意） | 元数据字段全部允许缺省，靠字典渐进治理 |
| 粘贴导入 + 手动新增是仅有的两条入口 | 转换器只需做 Xray→PWF 一个方向；Nuclei 导入降级 backlog |

### 1.2 格式定义

```yaml
apiVersion: pocwb/v1
kind: Poc
metadata:
  uid: poc_01J8ZK3Q...          # 工具生成 ULID
  name: XWiki SolrSearch RCE    # 必填
  aliases: [CVE-2025-24893]     # CVE/CNVD/内部编号
  severity: critical            # critical|high|medium|low|info，默认 info
  category: rce                 # rce|sqli|fileread|fileupload|unauth|weakpass|infoleak|ssrf|xxe|other
  vendor: XWiki                 # 字典外键，可空
  product: XWiki Platform       # 字典外键，可空
  tags: [cve, cve2025, xwiki]
  description: XWiki SolrSearch接口远程命令执行漏洞
  status: tested                # tested|untested|failed|faked|archived(软删除)
  source: xray                  # xray|manual|script（记录来源，纯信息用）
  created_at: 2026-08-21T08:00:00Z   # RFC3339 UTC，应用层生成
  updated_at: 2026-08-21T08:00:00Z
  last_tested_at: null
spec:
  transport: http               # http | tcp
  rules:
    r0:
      request:
        method: GET
        path: /bin/get/Main/SolrSearch?media=rss&text=%7d%7d%7d%7b%7basync...%20
        headers: {}             # 可选
        body: ""                # 可选，POST 时用
      expression: response.status == 200 && response.body.bmatches('root:.*:0:0') && response.body.bcontains(b'wiki')
    r1:
      request:
        method: GET
        path: /xwiki/bin/get/Main/SolrSearch?media=rss&text=...
      expression: response.status == 200 && response.body.bmatches('root:.*:0:0')
  expression: r0() || r1()      # 只能引用已声明的 rule 名 + &&/||/!/括号
```

**TCP 型示例（由你的 rsync 样例改写）：**

```yaml
spec:
  transport: tcp
  rules:
    req:
      request:
        inputs:                       # 按序收发
          - data: "@RSYNCD: 31.0\n\n"
        read_timeout: 3               # 秒
      expression: response.raw.bcontains(b'@RSYNCD: ') && response.raw.bcontains(b'@RSYNCD: EXIT')
  expression: req()
```

### 1.3 表达式运行时（自研引擎的求值基础）

`response` 对象按 transport 注入：

| transport | 字段 |
|---|---|
| http | `status`(int)、`headers`(map)、`body`(bytes)、`content_type`(string)、`raw`(bytes)、`elapsed_ms`(int64，请求网络耗时) |
| tcp | `raw`(bytes)、`elapsed_ms`(int64) |

内置函数集（expr-lang 注册，全部纯 Go 实现）：

| 函数 | 说明 |
|---|---|
| `bcontains(b'..')` / `contains('..')` | 字节/字符串包含 |
| `bmatches('re')` / `matches('re')` | 正则匹配——Go regexp=RE2 语义，线性时间，**天然免疫 ReDoS** |
| `bstartswith/bendswith/startswith/endswith` | 前后缀 |
| `tolower/toupper/len` | 常规工具 |

求值由 expr-lang 编译执行：表达式在**保存时编译校验**、运行时走编译缓存，非法语法/未声明 rule 引用在入库前就被拒绝。

---

## 二、存储设计（SQLite）

```sql
-- 连接初始化
PRAGMA foreign_keys = ON;
PRAGMA journal_mode = WAL;
PRAGMA user_version = 1;        -- store 层顺序迁移

CREATE TABLE vendor (
  id INTEGER PRIMARY KEY,
  canonical_name TEXT NOT NULL UNIQUE,
  aliases TEXT NOT NULL DEFAULT '[]',     -- JSON
  created_at TEXT NOT NULL
);

CREATE TABLE product (
  id INTEGER PRIMARY KEY,
  vendor_id INTEGER NOT NULL REFERENCES vendor(id) ON DELETE RESTRICT,
  canonical_name TEXT NOT NULL,
  aliases TEXT NOT NULL DEFAULT '[]',
  UNIQUE(vendor_id, canonical_name)
);

CREATE TABLE poc (
  uid TEXT PRIMARY KEY,                     -- ULID
  name TEXT NOT NULL,
  aliases TEXT NOT NULL DEFAULT '[]',
  severity TEXT NOT NULL DEFAULT 'info'
    CHECK (severity IN ('critical','high','medium','low','info')),
  category TEXT NOT NULL DEFAULT 'other',
  vendor_id INTEGER REFERENCES vendor(id) ON DELETE RESTRICT,
  product_id INTEGER REFERENCES product(id) ON DELETE RESTRICT,
  tags TEXT NOT NULL DEFAULT '[]',
  description TEXT NOT NULL DEFAULT '',
  cve TEXT,
  status TEXT NOT NULL DEFAULT 'untested'
    CHECK (status IN ('tested','untested','failed','faked','archived')),
  source TEXT NOT NULL DEFAULT 'manual'
    CHECK (source IN ('xray','manual','script')),
  spec_kind TEXT NOT NULL DEFAULT 'template'
    CHECK (spec_kind IN ('template','script')),
  spec TEXT NOT NULL,                       -- PWF YAML 全文（template）；脚本全文（script，不可执行只管理）
  spec_sha256 TEXT NOT NULL UNIQUE,         -- 规范化序列化后哈希 = 内容去重键
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL,
  last_tested_at TEXT
);

CREATE INDEX idx_poc_status   ON poc(status);
CREATE INDEX idx_poc_severity ON poc(severity);
CREATE INDEX idx_poc_vendor   ON poc(vendor_id);
CREATE INDEX idx_poc_cve      ON poc(cve) WHERE cve IS NOT NULL;

CREATE TABLE test_run (
  id INTEGER PRIMARY KEY,
  poc_uid TEXT NOT NULL REFERENCES poc(uid) ON DELETE CASCADE,
  target TEXT NOT NULL,                     -- 已校验 http/https URL
  target_host TEXT NOT NULL DEFAULT '',
  result TEXT NOT NULL CHECK (result IN ('hit','miss','error','timeout','cancelled')),
  log TEXT NOT NULL DEFAULT '',             -- 截断 5MB + 脱敏后落库
  authorized INTEGER NOT NULL,              -- RunTest 参数落库留痕
  started_at TEXT NOT NULL,
  ended_at TEXT
);

CREATE VIRTUAL TABLE poc_fts USING fts5(
  uid UNINDEXED, name, aliases, tags, description, vendor, product,
  tokenize='trigram'
);
```

**store 层约定**（沿用评审结论）：单写连接 + WAL；FTS 同事务双写，字典改名自动刷新受影响行；PocSummary 显式列清单禁 `SELECT *`；时间戳一律 RFC3339 UTC 应用层生成；搜索 ≥3 字符走 trigram MATCH、<3 字符 LIKE 回退（全表扫描在个人库规模下毫秒级，可接受）。

> 与 v2 的差异：`raw/raw_filename/origin_path/raw_sha256/engine/risk_flags` 全部移除——没有原文托管、没有外部引擎、没有敌意模板直喂进程的问题面。

---

## 三、后端处理（Go + Wails v2）

技术栈不变：Wails v2.15.0 + modernc.org/sqlite v1.57.0（纯 Go 无 CGO）+ gopkg.in/yaml.v3 + **expr-lang/expr（新增）**。

### 3.1 目录结构

```
PoCWorkbench/
├── main.go                    # Wails 启动 + PRAGMA + 迁移
├── app/                       # 绑定方法层（薄）
├── internal/
│   ├── model/                 # PWF 结构体 + DTO
│   ├── store/                 # CRUD + FTS 双写 + 迁移 + 备份
│   ├── pwf/                   # PWF schema 定义 + 校验器 + 规范化哈希
│   ├── convert/               # xray.go：Xray YAML → PWF 草稿（唯一转换方向）
│   ├── engine/                # 自研执行引擎：http.go / tcp.go / exprfn.go / run.go
│   └── dict/                  # 厂商/产品字典 + 别名归并 + 建议
└── frontend/
```

### 3.2 入口一：粘贴 Xray 自动转换

```
前端粘贴 Xray YAML（上限 256KB）
  → ConvertXray(yamlText)          # 纯函数，不落库
      解析（2s 超时 + recover 兜底）
      → 字段映射（见 3.3）→ PWF 草稿 + 警告列表
        （vendor/product 缺失→建议 UNKNOWN；severity 缺失→info；
         从 name/proof.info 抽取 CVE/CNVD 进 aliases）
  → 前端展示草稿表单（元数据可改、spec 可视）
  → 用户确认 → CreatePoc(draft)
      校验三关：
      ① schema 校验（必填/枚举/结构）
      ② 全部 expression 编译校验（expr-lang，含函数注册表）
      ③ 最终 expression 只引用已声明 rule 名
      → 规范化序列化 → sha256 去重 → 入库 + FTS
```

### 3.3 Xray → PWF 映射表

| PWF | Xray 来源 | 缺省策略 |
|---|---|---|
| metadata.name | `name`（清洗 `yingyongruanjian-` 类前缀） | 空则用 `id` |
| metadata.aliases | `[id]` + name/proof.info 正则抽 CVE/CNVD | 至少含原 id |
| metadata.severity | —（Xray 无此字段） | `info`，人工改 |
| metadata.category | proof.info 关键词词表推断 | `other` |
| metadata.vendor/product | `detail.fingerprint.company/product` | 空→UNKNOWN 待治理 |
| metadata.tags/description | `tags` / `detail.vulnerability.proof.info` | 空 |
| spec.transport | `transport`（缺省按 http 处理） | — |
| spec.rules | `rules` 近 1:1（request 字段同名；expression 原样保留——本来就是 Go 语法） | — |
| spec.expression | `expression` 原样 | — |
| 丢弃项 | `detail.fingerprint`（已提升为元数据）、`set`/`output`/`needreverse` 等 Xray 特有字段 | 警告列表明示 |

> 转换是有明确边界的：表达式语法同源所以近乎无损；Xray 冷门特性（`set` 变量、`output.matches`）v1 不支持，命中即在警告列表中列出，绝不静默丢弃。

### 3.4 入口二：手动新增（表单逐项填写）

前端四步向导（见 §4.2），提交同一个 `CreatePoc(draft)`，与粘贴路径共享全部校验逻辑：

1. 元数据表单：name/severity/category/tags/CVE/description；vendor/product 输入框带字典自动补全（输入即触发 SuggestVendorProduct）
2. transport 选择：http / tcp
3. Rule 构建器：每条 rule = 请求编辑器（http: method/path/headers 键值表/body 多行框；tcp: inputs 列表 + read_timeout）+ expression 输入框（旁置函数速查表，失焦即时编译校验显示 ✓/✗）
4. 总 expression：输入框 + 校验（只允许 rule 名与布尔运算符），保存前编译验证

脚本类 PoC（py/go/sh/java）：`spec_kind='script'`，表单只有元数据 + 代码粘贴框，不可执行、只做存储检索管理。

### 3.5 绑定方法（全部 API）

```go
// 创建/编辑（两条入口共用）
ConvertXray(yamlText string) (PwfDraft, error)   // 粘贴转换预览，不落库
CreatePoc(draft PwfDraft) (uid string, error)    // 三关校验后入库
UpdatePocSpec(uid string, spec PwfSpec) error    // 改模板体（重新过三关校验）
UpdatePocMeta(uid string, meta WkbMeta) error    // 改元数据（同步刷 FTS）

// 查询
ListPocs(filter PocFilter, page, pageSize int, sort SortKey) (Paged[PocSummary], error)
GetPoc(uid string) (Wkb, error)
SearchPoc(q string, filter PocFilter, page, pageSize int) (Paged[PocSummary], error)

// 删除（软删优先）
ArchivePoc(uid string) error
RestorePoc(uid string) error
DeletePoc(uid string) error                      // 物理删除仅限 archived

// 字典
ListVendors() []Vendor
ListProducts(vendorID int) []Product
MergeVendorAlias(canonical string, alias string) error
SetPocVendorProduct(uid string, vendorID, productID int) error
SuggestVendorProduct(text string) []Suggestion   // 自动补全数据源

// 验证（自研引擎）
RunTest(uid, target string, authorized bool) (runID int, error)
ListTestRuns(uid string) ([]TestRun, error)
GetTestRun(runID int) (TestRun, error)
CancelTest(runID int) error

// 维护
Dashboard() DashboardStats
BackupDB() (string, error)                       // VACUUM INTO 时间戳文件
```

### 3.6 自研执行引擎（internal/engine）

```
RunTest(uid, target, authorized)
  门禁：authorized=false 拒绝；target url.Parse 校验 scheme∈{http,https} 且 host 非空
  → 取 PWF spec → 编译缓存取表达式
  → 全局并发=1 队列；单次硬超时 60s（可配）
  → 按 spec.expression 的依赖图惰性求值各 rule（短路：|| 左真不跑右）
      http rule：net/http 客户端，base=target+path，注入 method/headers/body
                 关闭重定向跟随（防模板把请求引向第三方）；响应读上限 10MB
      tcp rule ：net.Dial(target host:port) → 按 inputs 顺序写/读，read_timeout 生效
  → 命中判定 → result ∈ hit|miss|error|timeout
  → 日志（请求/响应摘要）截断 5MB + password/token/secret 正则脱敏 → 落 test_run
  → 回写 poc.status / last_tested_at
```

安全属性（相对 v2 大幅简化）：
- **无外部进程**——进程注入/临时文件/引擎路径校验整类问题消失
- **能力天然收敛**——引擎只实现 HTTP/TCP 收发+表达式判断，不存在 interactsh 外带、code protocol 执行命令的面
- 正则 RE2 线性时间，无 ReDoS；表达式求值有 60s 硬顶
- 保留的门禁：授权确认后端强制、目标 scheme 校验、并发=1、超时 kill、日志脱敏

---

## 四、前端设计（React 19 + TS + Vite + Wails）

### 4.1 页面总览

| 页面 | 内容 |
|---|---|
| **总览** | 计数卡片：status 四态、severity 分布、vendor Top10 |
| **PoC 库** | Ctrl+K 搜索 + 多维筛选（厂商/产品/severity/category/status/source）+ 可排序分页表格；行点击进详情 |
| **PoC 详情** | 三区：① 元数据卡片（就地编辑）② spec CodeMirror 只读高亮 ③ 测试历史（ListTestRuns + 日志文本渲染） |
| **新增 PoC** | 双 Tab：「粘贴 Xray 转换」/「手动创建」，见 4.2 |
| **编辑 PoC** | 与新增共用表单组件，预填现有值 |
| **字典管理** | 厂商/产品两级列表、别名归并、UNKNOWN 治理队列 |
| **验证测试** | 选 PoC → 填目标 → 勾授权 → 执行 → 实时日志 + hit/miss 结果 |
| **设置** | 备份按钮、默认超时/并发配置、DB 风险提示常驻文案 |

### 4.2 新增 PoC 页（你的两条路径）

```
┌────────────────────────────────────────────────────┐
│  [Tab: 粘贴 Xray 转换]  [Tab: 手动创建]              │
├────────────────────────────────────────────────────┤
│ Tab A                                              │
│  ┌──────────────────────────┐                      │
│  │ 粘贴区 (CodeMirror yaml)  │  [解析预览]           │
│  └──────────────────────────┘                      │
│  → 草稿表单（元数据可改 + 警告列表黄条展示）           │
│  → [保存入库]                                       │
├────────────────────────────────────────────────────┤
│ Tab B（四步向导）                                    │
│  ① 元数据：name* / severity / category / tags /     │
│     CVE / 描述 / 厂商ⓘ自动补全 / 产品ⓘ自动补全        │
│  ② transport：○ http  ○ tcp                        │
│  ③ Rules：[+ 添加规则]                              │
│     ┌ rule r0 ────────────────────┐                 │
│     │ method ▾  path [________]   │                 │
│     │ headers 键值对表格 [+行]      │                 │
│     │ body [多行框]                │                 │
│     │ expression [______] ✓编译通过 │  函数速查 ▾      │
│     └─────────────────────────────┘                 │
│  ④ 总 expression [r0() || r1()] ✓引用合法            │
│  → [保存入库]                                       │
└────────────────────────────────────────────────────┘
```

两个 Tab 最终产出同一份 PWF 草稿、走同一个 `CreatePoc`——用户可以在粘贴转换后继续用表单微调，也可以全程手填。

### 4.3 展示细节

- 列表行：名称、CVE 徽章、severity 色块、vendor、status 图标、来源标签（xray/手动/脚本）
- 详情页 spec 高亮 + 长表达式折叠
- 同 CVE 多 PoC 关联跳转（idx_poc_cve）
- 测试日志强制 textContent 渲染（防 XSS→绑定方法滥用，验收项）

---

## 五、排期（v3：15 人日）

| 阶段 | 内容 | 工期 |
|---|---|---|
| P0 脚手架 | wails init、Vite+React+TS、Tailwind、git | 0.5d |
| P1 数据层 | DDL + 迁移 + store CRUD + FTS 双写 + BackupDB | 2d |
| P2 PWF 与转换器 | schema/校验器三关/规范化哈希 + Xray→PWF 映射（真实样例单测） | 2d |
| P3 执行引擎 | http/tcp 客户端 + expr 函数库 + 规则编排 + 超时/并发控制 | 3d |
| P4 API 绑定 | 全部方法 + TS 类型生成 | 1d |
| P5 前端核心 | 布局、列表搜索筛选、详情三区 | 2.5d |
| P6 新增/编辑 UI | 粘贴转换 Tab + 手动向导 + 字典管理 | 2d |
| P7 验证测试页 | 门禁 + 引擎调用 + 日志展示回写 | 1.5d |
| P8 打包 | exe、WebView2 检测、首次初始化 | 0.5d |
| **合计** | | **15 人日** |

依赖：P0→P1→P2→P3→P4 串行；P5 依赖 P4；P6/P7 并行；P8 收尾。
可选追加：存量 pocmeta2 目录批量迁移（扫目录→逐文件走 ConvertXray→批量入库+目录名初始化字典），+1d，待你确认要不要。

---

## 六、风险与对策

| 风险 | 对策 |
|---|---|
| Xray 冷门特性转换丢失（set/output 等） | 警告列表明示，绝不静默丢；命中特性按需迭代支持 |
| 表达式运行时异常 | 保存时编译三关拦截；运行时 recover + 60s 硬顶 |
| 目标误打 | scheme/host 校验 + 授权门禁后端强制 + 重定向关闭 |
| 并发写冲突 | 单写连接 + WAL + 分批事务 |
| 字典改名 FTS 失效 | 字典方法内自动刷新受影响行 |
| 明文库泄露 | 设置页常驻提示 + 日志脱敏；加密评估过不做（modernc 无 SEE，字段加密毁 FTS） |
| trigram 短词查不到 | LIKE 回退，个人库规模代价可接受 |

---

## 七、与 v2 的差异一览

| 项 | v2 | v3 |
|---|---|---|
| 存储对象 | 元数据 + 原始文件全文托管 | **PWF 原生模板**（自有格式，无可引用文件） |
| 执行方式 | 调外部 nuclei/xray 二进制吃原文 | **自研 Go 引擎直接执行 PWF** |
| 新增 PoC | 依赖已有文件导入 | **粘贴 Xray 转换 / 表单手填**，零文件依赖 |
| Nuclei 支持 | 解析器 P2 | 降级 backlog（你需要时再加转换器） |
| 进程安全铁律 | 7 条 | 整类删除（无外部进程） |
| 工期 | 15.5d | 15d（引擎 +3d，但砍掉 nuclei 解析器与外部进程管理） |

## 八、待确认

1. 存量 pocmeta2 目录批量迁移要不要（+1d）？
2. PWF 是否需要后续加 Nuclei 粘贴转换（当前按 backlog，不做）？
